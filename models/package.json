{
  "name": "@somethingorg/cafeio-models",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "cleanbuild": "npm run clean && ./node_modules/.bin/tsc",
    "build": "./node_modules/.bin/tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish --access=public"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^6.6.3",
    "typescript": "^4.8.4"
  }
}
